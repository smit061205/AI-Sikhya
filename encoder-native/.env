# Native macOS Encoder Configuration
GCP_PROJECT_ID=dev-airlock-471717-b0
GCS_SOURCE_BUCKET=dev-airlock-471717-b0-vod-source
GCS_PUBLIC_BUCKET=dev-airlock-471717-b0-vod-public
PUBSUB_TOPIC=vod-uploads
PUBSUB_SUBSCRIPTION=vod-uploads-sub

# Callback configuration
ENCODER_CALLBACK_BASE=http://localhost:3000/api/admin
ENCODER_CALLBACK_SECRET=Mithu@smit

# Google Cloud credentials path
GOOGLE_APPLICATION_CREDENTIALS=../Backend/gcp-credentials.json

# Optimized Encoding settings for better performance
HLS_SEGMENT_SECONDS=6
MAX_CONCURRENT=3
ENABLE_PARALLEL_ENCODING=true
ENABLE_HARDWARE_ACCELERATION=true

# Performance optimization settings
FFMPEG_THREADS=0
FFMPEG_PRESET=fast
FFMPEG_TUNE=fastdecode
ENABLE_GPU_ACCELERATION=true

# Memory and resource settings
MAX_MEMORY_USAGE=8192
TEMP_DIR_CLEANUP=true
ENABLE_PERFORMANCE_LOGGING=true
